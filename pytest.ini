[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not oracle and not stress and not benchmark and not threshold"
markers =
    oracle: tests that require rating_original.py and its optional dependencies
    benchmark: pytest-benchmark runtime tests
    stress: long-running signal stress tests
    threshold: optimization threshold matrix benchmarks
