[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not benchmark"
markers =
    annex: tests that use official or generated Annex data
    benchmark: runtime benchmarks excluded from default pytest
filterwarnings =
    error
